# === CẤU HÌNH SCRIPT SẮP XẾP (sort_photos.py) ===

# Tên mô hình YOLO (ví dụ: yolov8n.pt, yolo11m.pt)
MODEL_NAME="yolo11x.pt"

# Ngưỡng tin cậy (ví dụ: 0.5 cho 50%)
CONFIDENCE_THRESHOLD=0.5

# Thiết bị chạy AI: 'mps' (GPU Mac), 'gpu' (NVIDIA), 'cpu' (CPU)
DEVICE="mps"


# === CẤU HÌNH TÊN THƯ MỤC VÀ FILE ===

# Tên thư mục chứa ảnh gốc
SOURCE_DIR_NAME="source"

# Tên thư mục chứa ảnh đã sắp xếp
OUTPUT_DIR_NAME="result"

# Tên file cơ sở dữ liệu JSON
DB_FILE_NAME="db.json"

# === CẤU HÌNH TĂNG TỐC ĐỘ XỬ LÝ ===
BATCH_SIZE=32
IMG_SIZE=640
USE_HALF_PRECISION=True
NUM_WORKERS=8


# Mac mini m4
# yolo11x + 0.6 + mps + 32/640/True/8 ~~ 335.80
# yolo12x + 0.6 + mps + 32/640/True/8 ~~ 374


# Colab GPU T4
# yolo12x + 0.5 + cuda + 16/1280/True/4 ~~ 849.44